# Secrets
.env
.env.local
.env.*.local

# Google service-account keys
ai-marketing-labs-*.json
*service-account*.json

# Dependencies
node_modules

# Next.js build output
.next
out

# TypeScript incremental build caches. These are machine-local and were
# previously committed, which caused phantom "File not found" errors when a
# stale cache disagreed with the working tree.
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# IDE / OS
.vscode
.DS_Store

# Archives
*.zip
*.7z

# Sibling scratch project — kept locally, never committed to the main repo
geo-app/